# 信号评级与交叉筛选 — Implementation Plan

> **Status:** ✅ 已完成

**Goal:** 将信号展示从二元 ▲/○ 改为星级评级（★），优选赔率页增加信号筛选，所有图表增加图例。

**Architecture:** 纯 UI 展示层改动，仅修改 `app.py` 中展示函数。`compute_odds_signals()` 已返回 `signal_count` 字段，只需修改消费端的展示逻辑。图例通过 `showlegend=True` + `legend=dict(orientation='h')` 实现。

**额外产出:** README.md 项目说明文档。

**Tech Stack:** Streamlit, pandas, Python 3

---

### Task 1: 优选赔率页 — 信号星级评等

**Files:**
- Modify: `app.py:1372-1403` (`render_odds_tab` 中的 `signal_map` 构造和 `fmt_signal` 展示)

- [ ] **Step 1: 修改 `signal_map` 改为包含 `signal_count` 字段**

找到 `render_odds_tab()` 中第 1372-1375 行：

```python
    signal_df = compute_odds_signals()
    signal_map = {}
    if not signal_df.empty:
        signal_map = dict(zip(signal_df['ts_code'], signal_df['signal']))
```

改为同时保存 `signal_count`：

```python
    signal_df = compute_odds_signals()
    signal_map = {}
    signal_count_map = {}
    if not signal_df.empty:
        signal_map = dict(zip(signal_df['ts_code'], signal_df['signal']))
        signal_count_map = dict(zip(signal_df['ts_code'], signal_df['signal_count']))
```

- [ ] **Step 2: 替换 `fmt_signal` 函数**

找到第 1396-1403 行：

```python
    def fmt_signal(code):
        s = signal_map.get(code, 'no_data')
        if s == 'buy':
            return '▲ 买点'
        elif s == 'no_data':
            return '—'
        return '○ 无'
    display['信号'] = display['ts_code'].apply(fmt_signal)
```

改为星级评级：

```python
    def fmt_signal(code):
        cnt = int(signal_count_map.get(code, -1))
        if cnt == 3:
            return '★★★'
        elif cnt == 2:
            return '★★☆'
        elif cnt == 1:
            return '★☆☆'
        elif cnt == 0:
            return '——'
        return '—'
    display['信号'] = display['ts_code'].apply(fmt_signal)
```

- [ ] **Step 3: 更新表头说明文字**

第 1379 行：

```python
    st.caption("按中位数Z降序排列，点击列头可切换排序。▲ 买点 = 当前触发信号，○ = 无信号")
```

改为：

```python
    st.caption("按中位数Z降序排列。★★★ = 三共振，★★☆ = 双确认，★☆☆ = 单信号，—— = 无")
```

- [ ] **Step 4: 验证**

```bash
streamlit run app.py
```

进入"优选赔率"Tab，确认信号列显示为 ★ 评级而非 ▲/○。

- [ ] **Step 5: Commit**

```bash
git add app.py
git commit -m "feat: 信号列改为星级评级 (★★★/★★☆/★☆☆/——)"
```

---

### Task 2: 行情跟踪页 — 信号汇总表增加星级

**Files:**
- Modify: `app.py:1287-1334` (`_render_signal_summary`)

- [ ] **Step 1: 重构信号汇总为每只股票一行，增加星级列**

当前 `_render_signal_summary` 展开为每只股票 × 每个指标一行（膨胀行）。改为每只股票一行，汇总三个指标的信号计数和最近信号日期。

替换 `_render_signal_summary` 函数体（第 1287-1334 行）：

```python
def _render_signal_summary(top_inds):
    """Show current signal status for all stocks, sorted by Z score."""
    z_map = {}
    if st.session_state.odds_df is not None and not st.session_state.odds_df.empty:
        for _, r in st.session_state.odds_df.iterrows():
            raw = r['ts_code'].split('.')[0]
            z = r.get('median_z', -999)
            if pd.isna(z):
                z = -999
            z_map[raw] = float(z)

    rows = []
    for code, df in st.session_state.stocks_daily.items():
        name = INDEX_CONSTITUENTS.get(code, code)
        z_val = z_map.get(code, -999)
        df = df.copy()
        df['trade_date'] = pd.to_datetime(df['trade_date'])
        df = df.sort_values('trade_date')
        recent_cutoff = df['trade_date'].max() - pd.Timedelta(days=90)

        sig_count = 0
        sig_names = []
        sig_dates = []
        for ind_name in top_inds:
            cfg = INDICATOR_REGISTRY.get(ind_name)
            if cfg is None:
                continue
            try:
                sig = cfg['func'](df, **cfg['params'][0])
                sig = filter_signals(df, sig)
            except Exception:
                continue
            recent = (df['trade_date'] >= recent_cutoff).values
            recent_sig = sig.values & recent
            if recent_sig.any():
                sig_count += 1
                sig_names.append(ind_name)
                last_idx = np.where(recent_sig)[0][-1]
                sig_dates.append(df['trade_date'].iloc[last_idx].date())

        if sig_count >= 3:
            stars = '★★★'
        elif sig_count == 2:
            stars = '★★☆'
        elif sig_count == 1:
            stars = '★☆☆'
        else:
            stars = '——'

        rows.append({
            '股票': name,
            'Z评分': f"{z_val:+.2f}" if z_val > -999 else '—',
            '信号': stars,
            '触发指标': ' + '.join(sig_names) if sig_names else '—',
            '最近信号日': max(sig_dates).strftime('%m-%d') if sig_dates else '—',
            '_z': z_val,
            '_cnt': sig_count,
        })

    if rows:
        rows.sort(key=lambda r: (r['_cnt'], r['_z']), reverse=True)
        df_rows = pd.DataFrame(rows).drop(columns=['_z', '_cnt'])
        st.dataframe(df_rows, use_container_width=True, hide_index=True)
    else:
        st.info("暂无信号数据")
```

- [ ] **Step 2: 验证**

```bash
streamlit run app.py
```

进入"行情跟踪"Tab，查看"当前信号汇总"表：每只股票一行，有"信号"星级列、"触发指标"列（列出具体触发了哪些指标）、"最近信号日"列。按信号数优先、Z 分次优先排序。

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: 信号汇总表改为星级展示，每只股票一行汇总"
```

---

### Task 3: 优选赔率页 — 增加"仅显示有信号"筛选

**Files:**
- Modify: `app.py:1377-1415` (`render_odds_tab` 中主排名表部分)

- [ ] **Step 1: 在 ranking table 上方增加 checkbox**

在第 1377 行 `st.subheader("券商个股赔率排名")` 之后，`display` DataFrame 构建之前，加入筛选控件：

```python
    # ---- Signal filter ----
    show_signals_only = st.checkbox("仅显示当前有信号", value=False, key='odds_signal_filter',
                                     help="勾选后只展示当前有买入信号的股票")

    # ---- Main ranking table ----
    st.subheader("券商个股赔率排名")
```

- [ ] **Step 2: 在 `st.dataframe` 调用前加入过滤逻辑**

在第 1405 行 `display = display.sort_values('median_z', ascending=False).reset_index(drop=True)` 之后、第 1410 行 `st.dataframe(...)` 之前，加入：

```python
    if show_signals_only:
        display = display[display['信号'] != '——']
```

- [ ] **Step 3: 过滤模式下显示统计**

在过滤逻辑之后、`st.dataframe` 之前加入统计提示：

```python
    if show_signals_only:
        st.caption(f"筛选结果: {len(display)} 只有信号的股票")
```

- [ ] **Step 4: 验证**

```bash
streamlit run app.py
```

进入"优选赔率"Tab，勾选"仅显示当前有信号"checkbox，确认表格只显示有 ★ 的股票，按 Z 排序；取消勾选恢复全表。

- [ ] **Step 5: Commit**

```bash
git add app.py
git commit -m "feat: 优选赔率页增加'仅显示有信号'筛选功能"
```

---

### Task 4: 整体验证

- [ ] **Step 1: 运行完整验证**

```bash
streamlit run app.py
```

验证清单：
- Tab 4（行情跟踪）：信号汇总表以星级展示，每只股票一行，按信号数+Z分排序
- Tab 5（优选赔率）：信号列显示 ★★★/★★☆/★☆☆/—— 星级；勾选筛选后可只看有信号的股票
- 页面上无 Python 报错

- [ ] **Step 2: 最终 commit（如有遗漏修改）**

```bash
git status
git diff
```
